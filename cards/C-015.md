# C-015 - Full toolbox parity, QA, and integration polish

status: todo
deps: C-014
implements: FR3, FR7, FR8 (full block integration QA)

## Scope

Finish the expanded block integration by making the UI, toolbox, generator, save/load, diagnostics, and sample programs coherent as one user-facing workflow. This card is the acceptance pass for all documented WhalesBot blocks.

1. **Toolbox parity** - every documented category and block appears in the Blockly toolbox with correct grouping, labels, field defaults, dropdowns, and output/statement shape.
2. **Workspace compatibility** - users can drag, connect, save, reload, and regenerate IR from representative blocks across every category.
3. **Diagnostics UX** - unsupported, telemetry-only, stub, and blocked-by-sandbox blocks show clear diagnostics in the app without breaking the rest of the program.
4. **Sample programs** - add or update sample programs that cover: line-following route, math/logic/control-flow, sensors, side-effect telemetry, variables/custom block, and C Code disabled/sandbox behavior.
5. **Regression suite** - add smoke coverage for Blockly -> IR -> Interpreter -> Telemetry -> Save/Load.
6. **Docs alignment** - update user-facing docs so every category has an implementation status and known limitation.
7. **Visual QA** - verify the full app layout remains usable with the larger toolbox on desktop and mobile-sized viewports.

## Allowed files

- `apps/client/src/blockly/*`
- `apps/client/src/components/*.tsx`
- `apps/client/src/store/*.ts`
- `apps/client/src/App.tsx`
- `apps/client/src/main.tsx`
- `apps/client/src/index.css`
- `packages/simulation-core/src/**/*.ts`
- `packages/simulation-core/test/**/*.ts`
- `apps/client/test/**/*`
- `docs/whalesbot-block-studio-dac-ta-block.md`
- `docs/block-registry.md`
- `docs/c-code-sandbox.md`
- `README.md`
- `maps/sample/*`

## Verify (run these before calling the card done)

- [ ] `pnpm --filter @htlab/simulation-core test`
- [ ] `pnpm --filter @htlab/client typecheck` or equivalent TypeScript check for the client package
- [ ] `pnpm --filter @htlab/client build` or equivalent production build
- [ ] Manual smoke: open app -> inspect every toolbox category -> drag representative blocks -> generate IR
- [ ] Manual smoke: save/reload a workspace containing at least one block from each category
- [ ] Manual smoke: run the expanded line-following sample and confirm telemetry updates
- [ ] Manual smoke: diagnostics are visible for stubs and blocked C Code
- [ ] Mobile-sized viewport check confirms toolbox and simulation surfaces remain usable

## Done-evidence (world-state proof, named BEFORE building)

- Test/typecheck/build outputs
- Screenshot of the full expanded toolbox with all categories
- Screenshot or recording of a workspace containing representative blocks from every category
- Save/load proof for a mixed-category workspace
- Telemetry/diagnostic excerpt showing implemented, telemetry-only, stub, and blocked-by-sandbox statuses

## Evidence (paste the actual proof here when done)

Pending; paste actual command outputs, screenshots, and smoke-test notes when the card is built.
