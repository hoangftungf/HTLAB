# C-013 - Variables and My Blocks execution model

status: done
deps: C-012
implements: FR3, FR7 (variables and custom blocks)

## Scope

Implement the variable and custom-block model needed for larger WhalesBot programs. This card should cover symbol tables, variable mutation, procedure definition/call, parameters, and return behavior in a deterministic interpreter model.

1. **Variable registry** - support create/get/set/change variable operations with stable ids and names persisted in Blockly XML and generated IR.
2. **Expression integration** - variables can be used in math, logic, sensor thresholds, wait conditions, loop counts, and motor power fields.
3. **My Blocks definitions** - support custom block declarations with typed parameters where Blockly exposes them.
4. **Call frames** - implement a simple call stack with local parameter values, return value handling, recursion guard, and max-call-depth guard.
5. **Return semantics** - wire `Return [value]` to custom-block calls; outside a callable context it should emit a diagnostic instead of crashing.
6. **Save/load compatibility** - project save/load must preserve variables, custom blocks, and generated IR.
7. **Tests** - cover variable mutation, nested custom-block calls, return value usage, and recursion/depth diagnostics.

## Allowed files

- `apps/client/src/blockly/blocks.ts`
- `apps/client/src/blockly/blockRegistry.ts`
- `apps/client/src/blockly/generator.ts`
- `apps/client/src/blockly/toolbox.ts`
- `apps/client/src/blockly/BlocklyEditor.tsx`
- `apps/client/src/store/projectStore.ts`
- `packages/simulation-core/src/interpreter/types.ts`
- `packages/simulation-core/src/interpreter/interpreter.ts`
- `packages/simulation-core/test/interpreter/*.test.ts`
- `apps/client/src/store/*.test.ts`

## Verify (run these before calling the card done)

- [x] `pnpm --filter @htlab/simulation-core test`
- [x] `pnpm --filter @htlab/client typecheck` or equivalent TypeScript check for the client package
- [x] Variable set/get/change works in interpreter tests
- [x] A custom block with one numeric parameter can be called from a program and affects robot/runtime state
- [x] Return value from a custom block can feed a math or motor field
- [x] Recursive or too-deep calls stop with diagnostic
- [x] Save -> reload -> generate IR preserves variables and custom block definitions

## Done-evidence (world-state proof, named BEFORE building)

- Test output for variables, calls, returns, and recursion guard
- IR snapshot for a program using one variable and one custom block
- Save/load proof showing the same generated IR before and after reload
- Diagnostic output for invalid `Return [value]` outside a callable context

## Evidence (paste the actual proof here when done)

Implementation summary:
- Variable v2 blocks now use Blockly `field_variable`, so XML preserves stable variable ids/names.
- Generator emits IR v2 `variables` and `functions` metadata, variable refs with `id/name`, `variable.change`, `function.call`, custom value calls, and return values.
- Interpreter supports global variables, call-frame locals, one-parameter custom block calls, return values, recursion guard, max-call-depth guard, and diagnostic return outside callable context.
- Toolbox exposes Create variable, Change variable, and My Blocks definition/call/parameter blocks.

Test output for variables, calls, returns, and recursion guard:
- `corepack pnpm --filter @htlab/simulation-core test`
- PASS: 9 test files, 149 tests.
- Includes `test/interpreter/c013-functions.test.ts` with 6 tests for variable set/change/get, function side effects, nested return calls, recursion guard, max call depth, and invalid return diagnostics.

IR snapshot proof:
- C-013 tests exercise IR v2 with:
  - `variables: [{ id: "var-speed", name: "speed", valueType: "Number" }]`
  - function `drive(power)` executing `motion.setMotorPair`
  - function `addTenth(x)` returning `x + 0.1`
  - nested call `twice(x) -> addTenth(addTenth(x))`
  - statement call node `function.call`
  - value call expression `{ kind: "call", callee: "twice", args: [...] }`

Save/load proof showing same generated IR before and after reload:
- `node_modules\.pnpm\node_modules\.bin\vitest.CMD run apps\client\src\store\projectStore.test.ts --environment jsdom --globals`
- PASS: 1 test.
- The test builds a Blockly workspace with variable id `var-speed` and custom block `double(x)`, saves XML + IR through `projectStore`, reloads XML into a new workspace, regenerates IR, and asserts:
  - `variables` is `[["var-speed", "speed"]]`
  - `functions` is `["double"]`
  - parameter `{ name: "x", valueType: "Number" }` is preserved
  - regenerated node ops match the original generated IR node ops

Diagnostic output for invalid `Return [value]` outside a callable context:
- Covered by `c013-functions.test.ts`.
- Diagnostic code: `HTLAB_RETURN_OUTSIDE_FUNCTION`.
- The same test confirms the next runtime command still executes, so invalid return does not crash or halt the main program.

TypeScript checks:
- `node_modules\.bin\tsc.CMD --noEmit -p packages\simulation-core\tsconfig.json` PASS
- `node_modules\.bin\tsc.CMD --noEmit -p apps\client\tsconfig.json` PASS
