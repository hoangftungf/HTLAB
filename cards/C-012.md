# C-012 - Sensor, Light Speaker, and AI compatibility blocks

status: todo
deps: C-011
implements: FR4, FR5, FR7 (sensor and side-effect compatibility)

## Scope

Add the non-motion block categories needed for toolbox/workspace parity while keeping runtime behavior honest. Blocks backed by simulator data should work; blocks without simulated hardware should produce telemetry/diagnostics rather than silently doing nothing.

1. **Sensor reporters and booleans** - implement integrated grayscale, single grayscale where mappable, ultrasonic placeholder distance, ambient light, temperature, humidity, flame, magnetic, volume, motor encoder, timer, color sensor, and touch/infrared booleans according to available simulator state.
2. **Timer and encoder state** - add runtime state for current timer, reset timer, motor encoder read, and reset motor encoder if not already available.
3. **Remote control stub** - implement `remote control button` as a documented boolean stub returning `false` with diagnostic/comment.
4. **Light Speaker side effects** - add telemetry-only commands for sound, LED RGB, LED off, emotion screen, digital tube, screen display, clear screen, clear digital tube, electromagnet, and `reading 1` stub.
5. **AI blocks** - include documented AI blocks in registry/toolbox with `stub` or `telemetry-only` status unless a real model/runtime exists.
6. **Telemetry events** - extend telemetry so side-effect blocks can be inspected without changing robot kinematics.
7. **Warnings** - unsupported hardware blocks must be visible in generated diagnostics and, where useful, the UI status panel.

## Allowed files

- `apps/client/src/blockly/blocks.ts`
- `apps/client/src/blockly/blockRegistry.ts`
- `apps/client/src/blockly/generator.ts`
- `apps/client/src/blockly/toolbox.ts`
- `apps/client/src/components/TelemetryPanel.tsx`
- `apps/client/src/store/simStore.ts`
- `packages/simulation-core/src/types.ts`
- `packages/simulation-core/src/sim.ts`
- `packages/simulation-core/src/interpreter/types.ts`
- `packages/simulation-core/src/interpreter/interpreter.ts`
- `packages/simulation-core/src/telemetry/*.ts`
- `packages/simulation-core/test/interpreter/*.test.ts`
- `packages/simulation-core/test/telemetry/*.test.ts`
- `packages/simulation-core/test/sensor/*.test.ts`

## Verify (run these before calling the card done)

- [ ] `pnpm --filter @htlab/simulation-core test`
- [ ] `pnpm --filter @htlab/client typecheck` or equivalent TypeScript check for the client package
- [ ] Sensor reporter tests cover grayscale, timer, encoder, and at least one placeholder hardware sensor
- [ ] Side-effect block tests prove telemetry events are emitted without changing robot pose
- [ ] `remote control button` and `reading 1` produce explicit stub diagnostics
- [ ] UI telemetry panel can display or list side-effect diagnostics/events

## Done-evidence (world-state proof, named BEFORE building)

- Test output for sensor and telemetry event behavior
- Telemetry excerpt showing LED/sound/display side-effect events
- Diagnostic output for `remote control button`, `reading 1`, and unsupported AI blocks
- Screenshot of the telemetry panel showing at least one side-effect or unsupported-block event

## Evidence (paste the actual proof here when done)

Pending; paste actual command outputs, telemetry excerpts, and screenshots when the card is built.
