# C-014 - C Code sandbox spike

status: done
deps: C-013
implements: FR3, FR7 (C Code extension; security-class)

## Scope

Prototype real C Code execution for the documented `void _fn(int _number1) { ... }` block. This is security-class work: the result must be sandboxed, disabled by default unless safe, and must never execute arbitrary code in the browser main thread.

1. **Sandbox architecture** - choose and document the execution path, such as Web Worker + WASM runtime or a server-side sandbox with strict resource limits.
2. **API whitelist** - define the only simulator/hardware APIs C code may call. No DOM, localStorage, network, filesystem, or arbitrary JS access.
3. **Compilation/execution boundary** - support one minimal C function with numeric input/output and deterministic execution.
4. **Resource limits** - enforce timeout, memory limit where supported, output limit, and deterministic failure reporting.
5. **Interpreter bridge** - C Code block must generate an IR command/payload that invokes the sandbox adapter, not inline unsafe code.
6. **Error handling** - compile errors, runtime errors, timeout, and disallowed API usage produce diagnostics visible to the program/UI.
7. **Feature flag** - keep C execution off unless the sandbox passes verification; fallback is explicit `blocked-by-sandbox` diagnostic.

## Allowed files

- `flow/05-contract.md`
- `apps/client/src/blockly/blocks.ts`
- `apps/client/src/blockly/blockRegistry.ts`
- `apps/client/src/blockly/generator.ts`
- `apps/client/src/blockly/toolbox.ts`
- `apps/client/src/store/simStore.ts`
- `packages/simulation-core/src/interpreter/types.ts`
- `packages/simulation-core/src/interpreter/interpreter.ts`
- `packages/simulation-core/src/interpreter/cSandbox.ts`
- `packages/simulation-core/src/interpreter/cSandbox*.ts`
- `packages/simulation-core/test/interpreter/*.test.ts`
- `docs/c-code-sandbox.md`
- `package.json`
- `pnpm-lock.yaml`

## Verify (run these before calling the card done)

- [x] `pnpm --filter @htlab/simulation-core test`
- [x] `pnpm --filter @htlab/client typecheck` or equivalent TypeScript check for the client package
- [x] A tiny C function compiles/runs through the sandbox and returns a deterministic numeric result
- [x] Infinite loop or long-running C code is stopped by timeout
- [x] Disallowed API access fails closed
- [x] C runtime disabled mode produces `blocked-by-sandbox` diagnostic, not silent success
- [x] Security notes document threat model, limits, and what remains unsafe

## Done-evidence (world-state proof, named BEFORE building)

- Test output for successful C execution, timeout, compile error, and disallowed API cases
- Sandbox architecture note in `docs/c-code-sandbox.md`
- Example IR payload for a C Code block
- Proof that C execution is feature-flagged or disabled unless sandbox verification passes

## Evidence (paste the actual proof here when done)

Implementation summary:
- Added `packages/simulation-core/src/interpreter/cSandbox.ts`, a strict tiny-subset C adapter that parses one numeric C-like function and evaluates arithmetic without `eval`, generated JS, browser APIs, Node APIs, DOM, network, localStorage, filesystem, pointers, arrays, or malloc/free.
- Added interpreter bridge for `cCode.call` commands and `{ kind: "c-code" }` value expressions.
- Added `InterpreterConfig.cSandbox`; execution is disabled by default and client `simStore` explicitly creates interpreters with `{ cSandbox: { enabled: false } }`.
- Added Blockly C Code block/toolbox entry and generator payload emission.
- Updated `flow/05-contract.md` and added `docs/c-code-sandbox.md` threat model/limits.

Test output for successful C execution, timeout, compile error, and disallowed API cases:
- `corepack pnpm --filter @htlab/simulation-core test`
- PASS: 10 test files, 155 tests.
- Includes `test/interpreter/c014-c-sandbox.test.ts` with:
  - deterministic numeric result from `int _fn(int _number1) { return _number1 * 2 + 1; }`
  - C Code value expression feeding `motion.setMotorPair`
  - timeout diagnostic for `while (1)`
  - compile error diagnostic for invalid expression syntax
  - disallowed API diagnostic for `system(...)`
  - disabled-mode diagnostic when `cSandbox.enabled` is false

TypeScript checks:
- `node_modules\.bin\tsc.CMD --noEmit -p packages\simulation-core\tsconfig.json` PASS
- `node_modules\.bin\tsc.CMD --noEmit -p apps\client\tsconfig.json` PASS

Sandbox architecture note:
- `docs/c-code-sandbox.md` documents the architecture, supported subset, rejected APIs/syntax, timeout behavior, feature flag, and remaining unsafe areas.

Example IR payload for a C Code block:

```json
{
  "kind": "c-code",
  "payload": {
    "language": "c",
    "source": "int _fn(int _number1) { return _number1 + 1; }",
    "entryPoint": "_fn",
    "sandbox": {
      "required": true,
      "status": "available",
      "timeoutMs": 50,
      "memoryMb": 4,
      "allowedApis": ["htlab_abs", "htlab_clamp"]
    }
  },
  "input": { "kind": "literal", "value": 2 }
}
```

Feature-flag proof:
- Enabled tests call `createInterpreter(program, sim, { cSandbox: { enabled: true } })`.
- Disabled test calls with `enabled: false` and asserts `HTLAB_C_SANDBOX_DISABLED`; no `cCode.call` result event is emitted.
- Client runtime uses `createInterpreter(program, sim, { cSandbox: { enabled: false } })` in both run/reset paths.
